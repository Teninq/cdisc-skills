# Generic agent installation

Use this target when your agent runtime does not have dedicated plugin support.

```bash
npx superpowers-skills install --target generic
```

Optional custom location:

```bash
npx superpowers-skills install --target generic --config-dir /path/to/skills
```

Validate layout:

```bash
npx superpowers-skills validate --target generic
```
