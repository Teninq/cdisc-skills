# Codex installation

Preferred install path:

```bash
npx superpowers-skills install --target codex
```

Validate and sync:

```bash
npx superpowers-skills validate --target codex
npx superpowers-skills sync --target codex
```

By default, skills are installed to `~/.agents/skills`.
