# OpenCode installation

Preferred install path:

```bash
npx superpowers-skills install --target opencode
```

Validate and sync:

```bash
npx superpowers-skills validate --target opencode
npx superpowers-skills sync --target opencode
```

Fallback: manual clone/symlink remains supported for legacy setups.
